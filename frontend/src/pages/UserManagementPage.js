import React from 'react';
import UserManagement from '../components/UserManagement'; // 引入已存在的 UserManagement 組件

const UserManagementPage = () => {
    return (
        <div>
           
            <UserManagement />
        </div>
    );
};

export default UserManagementPage;
